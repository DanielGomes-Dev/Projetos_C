Exercícios para a aula de laboratório:

1) Faça uma função que some todos os elementos de nível ímpar de uma árvore binária simples e subtraia todos os de nível par - int misc1(TAB *a).

2) Faça uma função que encontra a média simples dos elementos em cada nível de uma árvore binária simples — float *media(TAB *a, int *tam_vet).

3) Faça uma função que encontre todos os elementos maiores que N  em uma ABB e retorne num vetor ordenado; não utilize nenhum algoritmo de ordenação - int *maioresN(TABB *a, int N, int *tam_vet).

4) Faça uma função que encontre todos os elementos menores que N em uma ABB e os retorne em um vetor ordenado; não utilize nenhum algoritmo de ordenação - int *menoresN(TABB *a, int N, int *tam_vet).

5) Retorne o nó de maior nível que é ancestral tanto de N quanto de M - TAB* maior_nivel(TAB *a, int N, int M).

6) Retorne o número de caminhos que somam N em uma árvore binária simples - int quant_caminhos(TAB *a, int N).

7) Faça uma função que determine se um grafo não direcionado é cíclico — int dir_ciclico(TG *g).

8) Faça uma função que determine se um grafo não direcionado é conectado (int dir_conect(TG *g))


---

Exercícios para a aula de laboratório:

(Q1) Retornar todas as informações ancestrais de um nó na árvore binária, da raiz da árvore até o elemento passado como parâmetro, usando a biblioteca de lista encadeada - TLSEINT* misc(TAB *a, int elem), onde:

typedef struct lseint{
  int lim_inf, lim_sup;
  struct lseint *prox;
} TLSEINT;

(Q2) Reescreva (Q1) com TABB;

(Q3) Escreva uma função que receba uma árvore binária e teste as seguintes propriedades: (a) se o nó está no nível par, ele tem ZERO ou dois filhos; e (b) se o nó está no nível ímpar, ele só pode ter UM filho. A função retorna UM se a árvore atender a essas propriedades e ZERO, caso contrário. A função deve ter o seguinte protótipo: int teste_arv(TAB* a); e

(Q4) Refaça (Q3) com TABB.

(Q5) Exercícios de grafos do Instagram e do Facebook.

(Q6) Dado um grafo conectado, verifique se ele pode ser transformado em uma árvore binária. Se ele for uma árvore binária, retorne um. Se não, retorne zero. DICA: use TLSE para resolver esta questão: int teste (TG *g); e

(Q7) Dado um grafo e dois nós (x e y), retorne um caminho qualquer entre x e y. Se não tiver caminho, o retorno é NULL: TLSE *caminho (TG *g, int x, int y).